# DigitalOcean Deployment Profile

- App Platform or Droplet with Docker Compose.
- Managed PostgreSQL.
- Managed Redis.
- Spaces for S3-compatible backups and exports.
- Load Balancer with TLS for SaaS domains and subdomains.

Use `docker-compose.yml` as the starting point, then replace local services with managed connection strings.
